# V7.4 통합 반응형 패치

- 클래식, 카오스 러시, 타임 크런치에 동일한 모바일 뷰포트 계산 적용
- 특정 아이폰 높이 기준 CSS 제거
- 실제 `visualViewport` 너비와 높이에 맞춰 보드와 현재 블록 영역 계산
- 카오스/타임 모드의 `overflow:hidden` 강제 잠금 해제
- 화면이 짧으면 단계적 압축, 그래도 부족하면 스크롤 방식으로 안전 전환
- Safari 주소창 변경, 화면 회전, 앱 복귀 시 다시 계산
- 320×568~430×932 검증
